[build]
builder = "python"
buildCommand = "pip install uv && cd Physical_Humanoid_AI_Robotics/backend && uv sync"

[deploy]
startCommand = "cd Physical_Humanoid_AI_Robotics/backend && uv run api_server.py"

